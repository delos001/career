# cv-targeted Skill Audit (2026-06-01)

Working ledger for the full audit of the cv-targeted skill, its agents, scripts,
rules, and interfaces. Objective: a stable, effective process that ingests
upstream outputs and produces an interview-winning CV, with appropriate artifacts
and minimal bloat. Criteria: (1) errors, (2) logical faults, (3) dead paths,
(4) gaps, (5) upstream-ingest integrity, (6) downstream enablement,
(7) artifact appropriateness & writeback, (8) objective alignment, (9) bloat &
redundancy, (10) internal consistency.

Disposition: `open` (surfaced) / `agreed` (fix confirmed) / `fixed` / `rejected`
(not a defect) / `deferred`.

| ID | Crit | File | Finding | Fix direction | Disp |
|----|------|------|---------|---------------|------|
| F1 | 5,1 | SKILL.md Phase 1 | Reads `## Axis Classification` (five axis values + level) from `research.md`, but role-intake writes that section only to the **session log**; research.md has no such section (only `## Axis Gaps`). Phase 1 cannot resolve level or axis files; the whole run fails on first real use. retrieval.md:59 already reads it from the session log as the authoritative source. | Phase 1 reads `## Axis Classification` from `personal/sessions/<SLUG>_APP-NNN_YYYY-MM_SessionLog.md` (matching retrieval), handling primary/secondary per axis. No upstream change. | fixed |
| F2 | 4,10 | SKILL.md Phase 1 | Never states that `inventory.md` / `narratives.md` / `user-info.md` live in `personal/profile/` (vs the application folder); executor must guess. gap-analysis qualifies them as profile-folder files. | Option A: qualify the three as profile-folder files in prose, matching gap-analysis. (Config-driven path resolution across all skills deferred: `skills-config-driven-paths`.) | fixed |
| F3 | 4,8 | qc-cv-targeted.md | 3 of 7 AI-tell categories (rule-of-three, trailing significance clause, Title Case/decorative bold) had no verifier: cv_qc.py covers only the mechanical subset, qc-cv-targeted excluded all AI-tells. Judgment subset unchecked despite being a hard rule central to the interview objective. | Added check 6 to qc-cv-targeted (judgment subset only, scoped as complement of cv_qc.py F2); refined its out-of-scope line; updated SKILL.md Phase 4b + routing table. | fixed |
| F4 | 3,4,10 | SKILL.md + hiring-manager.md | "Suspected extraction miss" path half-wired: routing table + hiring-manager step 3 declared it, but the return JSON had no field for it (domain enum lacked it), so the signal fell into the Phase 3 integrate loop and was lost or mislabeled at handoff. | Decided: extraction miss = a critical JD element absent from BOTH the requirements and an already-identified gap (refined condition); Option A (cv-targeted surfaces only, no inline resolution, no auto re-run); b1 generic advisory handoff wording. Added `suspected_extraction_misses[]` channel to hiring-manager return (independent of verdict/contributions); Phase 3a collects it separately; Phase 3e logs it deduped; Phase 6 surfaces it as its own advisory line; routing table corrected. | fixed |
| F5 | 8,10 | cv-structure.md + cv-best-practices.md + cv_qc.py | Strongest empirical best-practice (Kristal tenure-as-years, +15%/+8% callbacks) is absent from the structure rule it grounds: cv-structure.md:117 mandates date-range headers only, no tenure-as-years option; cv_qc.py N1 assumes/parses date ranges. Evidence-to-rule gap. | Real gap but resolving it well is a cross-artifact CV-format design decision (additive option, not replacing ranges; touches structure + N1 + render). Not blocking. Logged deferral `tenure-as-years-rendering` (triggers at cv-render design). | deferred |
| F8 | 5,10 | SKILL.md + cv-architect.md + hiring-manager.md | cv-targeted mis-attributed the CR-NNN id space to research.md, but research.md carries no stable id (Text/Type/Source per template; takeda added a positional `#`). CR-NNN is created downstream by gap-analysis (assigned in research's list order, 1:1). retrieval.md (pre-gap-analysis) references requirements by bare position. cv_qc.py C4 already validates CR markers against gap_analysis.md, so the script was right and only the prose misdirected the architect (risking C4 failures / wasted fix-loop iterations). Session note's premise ("research.md requirements are CR-NNN") was wrong; corrected. | Fixed: SKILL.md:71 now says research.md requirements are text/type with no stable id; the gap_analysis read bullet names CR-NNN as gap-analysis's authoritative id space mapping positionally to research + retrieval's bare numbers; cv-architect.md:29/31 corrected (also dropped the inaccurate "axis classification in research.md" residual from F1; axis info comes from the passed axis files); hiring-manager.md:30 corrected to source CR-NNN from gap_analysis.md. Flagged out-of-scope: role-intake research_file.md template (Text/Type/Source) vs takeda output (`#` column) drift, for role-intake. | fixed |
| F9 | 10 | cv-structure.md (Scope boundary) | Fresh-scan sweep found two contradictions left by F6/F8 in the Scope-boundary section: (1) line 440-441 said this file "does not own ... Earlier Professional Roles threshold and density - governed by [slug]," directly contradicting F6 which moved that spec into the file (lines 134-160); (2) line 438 said critical-requirements are "produced by role-intake (CR-NNN)," repeating the F8 misattribution (CR-NNN is gap-analysis's id space). | Fixed: removed the contradictory "does not own threshold/density" bullet (file now owns it inline; only visual rendering is deferred to cv-render, clarified); corrected the extraction bullet to "role-intake (text/type); CR-NNN assigned by gap-analysis." Confirmed no runtime file depends on the trimmed slug. Other fresh-scan targets (career-strategist, candidate-advocate, qc-cv-targeted checks 1-6, cv_qc.py constants vs cv-structure.md limits) verified clean and consistent. | fixed |
| F7 | 9 | career-strategist.md + hiring-manager.md + candidate-advocate.md | "Responding to the architect" advisor-conduct boilerplate (~7 lines) near-identical across the three stakeholders, with light per-agent tailoring. | Rejected: not runtime bloat (isolated agent contexts each load only their own copy); no include mechanism exists for agent prompts so no clean consolidation; per-agent tailoring is appropriate; distinct from `bloated-skill-architecture-revisit` (SKILL scaffolding). User concurs redundancy is real but structural, not solvable via one global rule. | rejected |
| F6 | 4,10 | cv-structure.md + cv-architect.md + cv_qc.py (+design_decisions.md) | Earlier Professional Roles under-wired: (A) its operational spec (10-yr threshold, inclusion rules 1-4, format) lived only in design_decisions.md, which the skill never loads at runtime, and cv-structure.md:137 deferred to it ("not restated here") so the architect could only guess; (B) absent from cv-architect's section enumeration + output contract; (C) unrecognized by cv_qc.py so S1 never validated its placement. Will render on real senior-candidate runs. | Fixed all: (A) moved operational spec into cv-structure.md (runtime home), trimmed design_decisions.md to a pointer + kept relevance-source/traceability rationale; (B) added the section to cv-architect.md composition step + output contract (plain Company\|Title\|Dates lines, no citation needed); (C) added `earlier professional roles` to cv_qc.py EVIDENCE_BAND after professional experience so S1 validates placement. cv_qc.py recompiles clean. | fixed |
