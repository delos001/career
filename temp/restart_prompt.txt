Resume career repo work. Apply the session-start protocol in memory `process_session_protocol.md`.

Quick reminder of where things are:
- `temp/design_decisions.md`: closed decisions.
- `temp/deferrals.md`: deferred items.
- `temp/open_questions.md`: blocking questions.
- `project_session_resume.md` (memory): planned next action.

Two open questions are gating before any foundation work proceeds: `foundation-execution-order` and `knowledge-doc-update-mechanism`. Resolve both with the user first.

Verify current repo state (filesystem, especially `personal/knowledge/`) before asking the user about it.
